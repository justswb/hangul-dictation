{
  "name": "hangul-dictation",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run",
    "validate:jamo": "node scripts/validate-jamo.ts",
    "check": "tsc --noEmit && eslint . && vitest run",
    "shot": "node scripts/shot.ts",
    "fixtures:expected": "node scripts/fixtures-expected.ts"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^26.5.1",
    "eslint": "^10.10.0",
    "happy-dom": "^20.14.5",
    "playwright": "1.63.0",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.70.0",
    "vite": "^8.3.0",
    "vitest": "^5.0.1"
  }
}
