# AI 화이트보드 — MVP 스펙

> 버전 0.3 · 2026-09-15 · 상태: 초안

**참고 문서**
| 문서 | 내용 |
|---|---|
| [MILESTONES.md](MILESTONES.md) | 마일스톤·작업 항목 |
| [docs/ui.md](docs/ui.md) | 화면 구성 |
| [docs/board-ops.md](docs/board-ops.md) | AI 출력 형식 (보드 명령) |
| [docs/prompt.md](docs/prompt.md) | 시스템 프롬프트 요지 |
| [docs/layout.md](docs/layout.md) | 레이아웃 규칙 |
| [docs/errors.md](docs/errors.md) | 오류 처리 |
| [docs/dev-env.md](docs/dev-env.md) | 로컬 개발 환경 |

## 1. 목표

동료와 회의실 화이트보드 앞에 선 느낌을 재현한다. 사용자가 질문하면 AI가 **말로 답하는 대신 화이트보드에 직접 쓰고 그리듯** 답한다.

**MVP 성공 기준**
1. 질문 전송 후 **2초 안에** 첫 획이 그려지기 시작한다(스트리밍).
2. 모든 AI 출력(글자·도형·화살표)이 페이드인·타자기 효과가 아니라 **획 단위로** 그려진다.
3. 한국어·영어 혼합 답변이 판서처럼 읽힌다(키워드, 짧은 줄, 간단한 도식).
4. 후속 질문 시 AI가 보드에 이미 있는 내용을 가리키거나(밑줄·동그라미·화살표) 이어서 쓴다.

## 2. 범위
아래 표에 없는 기능은 MVP 범위 밖이다.

| # | 기능 |
|---|---|
| F1 | 하단 입력 박스에서 질문 입력·전송 |
| F2 | AI 응답을 보드 명령(ops)으로 스트리밍 수신 |
| F3 | 획 기반 필기 애니메이션 (글자·도형·화살표·강조) |
| F4 | 자동 레이아웃 (좌표 계산은 앱이 담당) |
| F5 | 쓰기 전에 공간이 부족하면 먼저 지우고 새 페이지에 쓴다 |
| F6 | 대화 맥락 유지 (현재 세션 내 후속 질문) |
| F7 | 중단 버튼 (생성·애니메이션 즉시 정지) |
| F8 | 보드 지우기 버튼 (보드 + 대화 맥락 초기화) |
| F9 | AI 제공자(Claude / OpenAI) 선택 + API 키 입력, OS 키체인 저장 |

## 3. 사용자 흐름

```
앱 실행 ─(키 없음)→ 제공자 선택 + API 키 입력 ─→ 빈 화이트보드
   │
   └→ 질문 전송
         → 입력 비활성, 상태 "생각 중…"
         → 첫 op 도착 → 쓰기 시작, 상태 "쓰는 중…"
         → 애니메이션 완료 → 입력 재활성
   (도중에 [중단] → 즉시 정지, 그려진 획은 남김, 입력 재활성)
```

## 4. 아키텍처

```
┌─────────── WebView (TypeScript) ────────────┐
│ InputBox → Session ──ops──▶ OpQueue          │
│   → LayoutEngine (op → 요소 + 좌표)           │
│   → StrokeGenerator (요소 → 획)               │
│   → Animator (rAF, Canvas 2D)                │
└──────────────▲──────────────────────────────┘
               │ OpSource 인터페이스
┌──────────────┴──── Rust core ───────────────┐
│ Provider (Claude | OpenAI) 스트리밍 호출       │
│ NDJSON 줄 파싱·검증 (제공자 공통)              │
│ API 키 키체인 보관 (WebView로 노출 안 함)       │
└─────────────────────────────────────────────┘
```

- 스택: **Tauri 2** + TypeScript(바닐라) + Canvas 2D, Rust 코어.
- **설계 원칙**: LLM은 좌표·획을 만들지 않는다. *무엇을 어떤 관계로* 그릴지만 말하고, 위치는 LayoutEngine, 획은 StrokeGenerator가 결정한다.
- **OpSource**: 프론트는 op 스트림만 받는다. 구현은 Tauri / 개발용 가짜 스트림 / 개발용 로컬 프록시로 교체 가능([docs/dev-env.md](docs/dev-env.md)).

## 5. AI 연동

| 제공자 | 모델 |
|---|---|
| Anthropic Claude API | 기본 `claude-opus-5` |
| OpenAI API | 추후 결정 |

- 공통 요구: 스트리밍, 텍스트로 보드 명령 출력([docs/board-ops.md](docs/board-ops.md)), 대화 이력 전달.
- 제공자별 오류·거절 응답은 Rust 코어에서 공통 오류 유형으로 변환한다([docs/errors.md](docs/errors.md)).
- **컨텍스트**: 시스템 프롬프트([docs/prompt.md](docs/prompt.md)) + 대화 이력(질문 / AI가 낸 ops 원문, append-only) + 매 질문 앞에 현재 페이지 요약(요소 id·종류·텍스트, 남은 공간).

## 6. 획 데이터

| 문자군 | 소스 |
|---|---|
| 영문·숫자·기본 기호 | Hershey Simplex 단선 폰트 |
| 한글 | **기본 자모 중심선 획 데이터 직접 제작 + 조합 규칙** |
| 미지원 문자 | `?` 획으로 대체 |

- 기본 자모 약 26자만 필순대로 단선 획으로 정의하고, 된소리·겹받침·복합모음은 이어 붙여 파생한다.
- 음절은 모음 형태 × 받침 유무 템플릿 6종에 자모를 배치해 만든다. 순서: 초성 → 중성 → 종성.
- 기존 공개 소스(hangulssugi, Hilokal/korean-handwriting, hangeul-stroke-order, animCJK, Hershey)는 라이선스·형식·한글 미지원 문제로 쓰지 않는다(2026-09 조사).

## 7. 비기능 요구사항
- 첫 획 지연 p50 ≤ 2s.
- API 키는 OS 키체인에만 저장, WebView·로그에 노출 금지.
- 텔레메트리 없음.

## 8. 추후 결정
- **배포**: CI, 설치 파일 형식, 코드 서명 — MVP 기능 완료 후 결정.
- **OpenAI 모델**, 제공자별 모델 파라미터 — M3에서 결정.
